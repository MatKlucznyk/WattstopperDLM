using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Crestron.SimplSharp;

namespace WattstopperDLM
{
    /*internal class RoomInterfaceAdded
    {
        private event EventHandler<RoomInterfaceAddedEventArgs> onNewEvent = delegate { };

        public event EventHandler<RoomInterfaceAddedEventArgs> OnNewEvent
        {
            add
            {
                if (!onNewEvent.GetInvocationList().Contains(value))
                {
                    onNewEvent += value;
                }
            }
            remove
            {
                if (onNewEvent.GetInvocationList().Contains(value))
                {
                    onNewEvent -= value;
                }
            }
        }

        internal void SendData(RoomInterfaceAddedinEventArgs e)
        {
            onNewEvent(null, e);
        }
    }*/
}